<section class="hero page-shell">
  <div class="content-shell">
    <div class="hero-shell inner-shell">
      <div class="hero-meta">
        <div>
          <p class="eyebrow">Index - 01</p>
          <p class="body-small">Independent design and brand studio. Founded 2019.</p>
        </div>
        <div>
          <p class="eyebrow">Locations</p>
          <p class="body-small">Brooklyn, NY, Lisbon, PT, and wherever the work is.</p>
        </div>
        <div>
          <p class="eyebrow">Disciplines</p>
          <p class="body-small">Identity, Digital, Editorial, Motion, Type</p>
        </div>
      </div>

      <h1 class="hero-title">
        <span class="plain">We design </span><span class="accent">brands</span><br />
        <span class="italic">for the in-between</span><br />
        <span class="plain">-</span><br />
        <span class="italic">half art,</span><span class="plain"> half engine.</span>
      </h1>

      <div class="hero-bottom">
        <p class="hero-copy">
          <strong>Halflight</strong> is an independent studio building considered identity systems, digital
          products, and printed matter for companies that refuse to look like the rest.
        </p>

        <a class="hero-cta" href="#">
          <span>See selected work</span>
          <span class="hero-cta-icon" aria-hidden="true">-&gt;</span>
        </a>

        <div class="hero-stat">
          <p class="hero-stat-number">84</p>
          <p class="eyebrow">Projects shipped</p>
          <p class="eyebrow">since 2019</p>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="status-strip page-shell">
  <div class="content-shell">
    <div class="status-shell inner-shell">
    <p class="eyebrow status-now">
      <span class="status-dot" aria-hidden="true"></span>
      <span>Currently - accepting two projects for Q3</span>
    </p>
    <p class="eyebrow">Next opening - Sept 2026</p>
    <p class="eyebrow">02:33 EST - Brooklyn, NY</p>
    </div>
  </div>
</section>

<style>
  .hero {
    padding-top: 20px;
  }

  .hero-shell {
    display: grid;
    gap: var(--section-gap);
  }

  .hero-meta {
    display: grid;
    gap: 24px;
    grid-template-columns: 1fr;
  }

  .hero-meta > div {
    display: grid;
    gap: 6px;
  }

  .hero-title {
    font-family: var(--font-serif);
    font-size: clamp(64px, 11.2vw, 200px);
    font-style: italic;
    font-weight: 400;
    letter-spacing: -0.02em;
    line-height: 0.9;
    margin: 0;
  }

  .hero-title .plain {
    color: var(--color-ink);
    font-style: normal;
  }

  .hero-title .accent {
    color: var(--color-accent);
    font-style: italic;
    font-weight: 400;
  }

  .hero-title .italic {
    color: var(--color-ink);
    font-style: italic;
  }

  .hero-bottom {
    align-items: end;
    display: grid;
    gap: 32px;
    grid-template-columns: 1fr;
  }

  .hero-copy {
    color: var(--color-ink-2);
    font-size: 18px;
    line-height: 25.2px;
    margin: 0;
    max-width: 480px;
  }

  .hero-copy strong {
    color: var(--color-ink);
  }

  .hero-cta {
    align-items: center;
    background: var(--color-ink);
    border-radius: var(--radius-pill);
    color: var(--color-cream);
    display: inline-flex;
    gap: 14px;
    height: 64px;
    justify-content: space-between;
    padding: 0 28px;
    width: fit-content;
  }

  .hero-cta span {
    font-size: 15px;
    line-height: 22.5px;
  }

  .hero-cta-icon {
    align-items: center;
    background: var(--color-cream);
    border-radius: 50%;
    color: var(--color-ink);
    display: inline-flex;
    font-size: 14px;
    height: 28px;
    justify-content: center;
    width: 28px;
  }

  .hero-stat {
    display: grid;
    gap: 6px;
    justify-items: end;
  }

  .hero-stat-number {
    color: var(--color-ink);
    font-family: var(--font-serif);
    font-size: 56px;
    font-style: italic;
    line-height: 1;
  }

  .status-strip {
    border-bottom: 1px solid var(--color-rule);
    border-top: 1px solid var(--color-rule);
    margin-top: 40px;
    padding-block: 15px;
  }

  .status-shell {
    align-items: center;
    display: grid;
    gap: 14px;
    grid-template-columns: 1fr;
  }

  .status-now {
    align-items: center;
    display: inline-flex;
    gap: 10px;
  }

  @media (min-width: 768px) {
    .hero-meta {
      gap: 40px;
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .hero-bottom {
      grid-template-columns: minmax(0, 1fr) auto auto;
    }
  }

  @media (min-width: 1024px) {
    .hero-shell {
      gap: 60px;
    }

    .hero-title {
      letter-spacing: -6px;
      line-height: 184px;
    }

    .hero-bottom {
      grid-template-columns: minmax(0, 1fr) 230.22px 116.17px;
    }

    .hero-cta {
      width: 230.22px;
    }

    .status-shell {
      gap: 16px;
      grid-template-columns: minmax(0, 1fr) auto auto;
      justify-content: space-between;
    }
  }
</style>
